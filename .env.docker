# Environment variables para Docker
# Copie este arquivo para .env e ajuste os valores

# Database
DB_NAME=kadesh
DB_USER=postgres
DB_PASSWORD=postgres_password_change_me
DB_PORT=5432

# Backend
NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Frontend URLs permitidas (CORS)
FRONTEND_URL=http://localhost
FRONTEND_URLS=http://localhost,http://localhost:80,http://frontend
