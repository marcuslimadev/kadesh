{
  "name": "kadesh-backend",
  "version": "1.1.0",
  "description": "API do Kadesh com PostgreSQL, carteiras e rotas administrativas",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:load-schema": "node scripts/load-schema.js",
    "migrate": "node scripts/migrate-data.js",
    "build": "node -e \"console.log('Render build hook: API ready after npm install, skipping build step.')\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.6.5",
    "pg": "^8.11.5",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
